# Security Policy

## 支持范围

当前仅维护 `main` 分支的最新版本。项目仍处于 Alpha 阶段，不承诺向后兼容。

## 报告漏洞

请不要在公开 Issue 中粘贴 API Key、访问令牌、私有仓库内容或可直接利用的敏感细节。优先使用 GitHub Security Advisory 的私密报告功能；如果该功能不可用，请只在 Issue 中说明需要私下联系，不要附带秘密信息。

报告请包含：受影响版本、最小复现步骤、预期与实际行为、影响范围，以及已经采取的密钥撤销或隔离措施。

## 运行边界

- CodePilot 可以读写文件并执行 Shell 命令，应仅在可信工作区运行。
- `bypassPermissions` 会跳过常规人工确认，不应作为日常默认值。
- Remote UI 默认监听 `127.0.0.1`；公开绑定前应增加网络隔离和身份认证。
- MCP、Hooks 与 Skills 属于外部代码执行边界，只使用可信配置和来源。
- `.codepilot/` 包含密钥、会话、记忆和日志，不得提交到版本库。

如果凭据已经泄露，请先在服务端撤销并轮换。删除工作区文件或改写 Git 历史不能让已泄露密钥恢复安全。
